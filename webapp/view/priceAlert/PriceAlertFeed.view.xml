<!DOCTYPE xml>
<mvc:View
	controllerName="trading-cockpit-frontend.controller.priceAlert.PriceAlertFeed"
	displayBlock="true"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:l="sap.ui.layout"
	xmlns:f="sap.ui.layout.form"
	xmlns:u="sap.ui.unified"
	xmlns:core="sap.ui.core">
	
	<Page title="{i18n>priceAlertFeed.header}">
		<content>
			<List items="{path: 'priceAlerts>/priceAlert/'}" >
				<FeedListItem text="{parts: [{path: 'priceAlerts>symbol'}, {path: 'priceAlerts>stockExchange'}], formatter: '.priceAlertTextFormatter'}"
					info="{path: 'priceAlerts>price', formatter: '.priceAlertInfoTextFormatter'}"
					timestamp="{path: 'priceAlerts>triggerTime', formatter: '.formatter.wsDateFormatter'}"
					icon="{path: 'priceAlerts>alertType', formatter: '.priceAlertIconFormatter'}">
					
					<FeedListItemAction text="{i18n>priceAlertFeed.confirmAlert}" icon="sap-icon://accept" press=".onActionPressed" />
				</FeedListItem>
			</List>
		</content>
	</Page>
</mvc:View>